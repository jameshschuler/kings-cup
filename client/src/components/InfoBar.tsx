import React from 'react';

const InfoBar: React.FC = () => {
	// TODO: show icon if connected or not

	return (
		<div id='info-bar'>
			<h3>HomeDoneGames</h3>
		</div>
	);
};

export default InfoBar;
